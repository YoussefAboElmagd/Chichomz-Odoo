<odoo>
    <data>
        <record model="ir.ui.view" id="view_contact_status_list">
            <field name="name">contact.status.list</field>
            <field name="model">customer.contact.status</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <!-- Add more fields as per your requirements -->
                </tree>
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_contact_status_list">
            <field name="name">Status</field>
            <field name="res_model">customer.contact.status</field>
            <field name="view_mode">tree,form</field>
        </record>

       
    </data>
</odoo>